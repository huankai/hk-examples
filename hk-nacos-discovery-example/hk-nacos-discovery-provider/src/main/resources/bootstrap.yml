spring:
  application:
    name: hk-nacos-provider
  main:
    banner-mode: "off"
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      # 注意，配置中心的参数必须在 bootstrap 上下文中
      config:
        enabled: true
        # 多个地址使用 , 分隔
        server-addr: 182.61.40.18:8848
        file-extension: yaml
